# リリース記録: v1.2.1

- リリース日: 2026-06-14
- 対象プロジェクト: earthQuake (Masakai/earthQuake)
- バージョン: v1.2.1
- 要件トレーサビリティ: なし（ユーザー指示によるリリース）
- 承認者: ユーザー（コミット・プッシュを明示指示）
- 関連レビュー報告書: なし（documents/reviews/ 未作成）
- 関連実装仕様書: なし
- 関連設計書: なし

## リリース内容

### Changed
- `dashboard.html`: トリガ閾値（STA/LTA）設定スライダーの上限を `max="10.0"` → `max="20.0"` に拡張。Web UI から閾値を最大20まで設定可能になった。
  - 背景: 閾値16で運用しているが従来の上限10では設定できなかったため。
  - サーバ側 `_CONFIG_LIMITS`（`jma_intensity_web.py`）の trig 上限は元々 (0.5, 50.0) で20を許容済みのため、変更はこの1箇所のみで完結。コード変更不要。

## リリース手順の実施記録

| 手順 | 実施内容 | 結果 |
|-----|---------|-----|
| バージョン番号更新 | バージョン管理ファイルなし（CHANGELOG.md / project_map.md で管理） | スキップ（設計上） |
| CHANGELOG追記 | `## [1.2.1] - 2026-06-14` を docs/CHANGELOG.md 先頭に追記 | 完了 |
| project_map.md 更新 | 現バージョン v1.2.0 → v1.2.1 | 完了 |
| gitコミット | コミットハッシュ: 7af2b1e | 完了 |
| タグ作成 | v1.2.1 | 完了 |
| プッシュ | origin/master | 完了 |
| GitHub Release | https://github.com/Masakai/earthQuake/releases/tag/v1.2.1 | 完了 |

## 特記事項

- 単一ファイル・1行のUI上限変更だが、プロジェクトの慣習（v1.1.1 も dashboard.html 単独のパッチリリース）に倣いパッチバンプ v1.2.1 としてリリースした。
- バージョン番号は VERSION ファイル・pyproject.toml が存在しないため docs/CHANGELOG.md と docs/project_map.md のみで管理。
- サーバ側 `_CONFIG_LIMITS` の trig 上限が (0.5, 50.0) で20を許容済みのため、本変更はクライアント側スライダー上限のみで完結しており追加のコード変更・テスト変更は不要。
- ロールバック手順: `git revert 7af2b1e` でCHANGELOG・project_map・dashboard.htmlを一括取消可能。タグは `git push origin :refs/tags/v1.2.1` で削除可能。
