# リリース記録: v1.4.0

- リリース日: 2026-06-16
- 対象プロジェクト: earthQuake (Masakai/earthQuake)
- バージョン: v1.4.0
- 要件トレーサビリティ: 発生直後の波形解析が公式FDSNの遅延（20〜30分）で空振りする運用上の課題への対応。専用チケットなし（運用者＝ユーザー起点の機能要求）
- 承認者: ユーザー（「リリースしてください」と明示承認）。動作確認はローカル・本番(iMac)両方でユーザー立ち会いのもと完了済み
- 関連レビュー報告書: なし（reviewer 報告書は未作成。ユーザー立ち会いの E2E 検証で代替）
- 関連実装仕様書: なし
- 関連設計書: なし
- 技術詳細の出典: `/Users/sakaimasanori/.claude/projects/-Users-sakaimasanori-Dropbox-earthQuake/memory/log_server-changes.md`（2026-06-16 の3エントリ）

## リリース内容

### Added
- `analyze_rs.py`: 公式FDSN（data.raspberryshake.org）にデータが無い場合、自局Raspberry Shake内蔵のSeedLink（`10.0.1.53:18000`、遅延約7秒）から波形を取得するフォールバックを追加
  - 背景: 公式FDSNは発生直後のデータが20〜30分遅れるため、発生直後の解析が空振りしていた
  - `download_channel` を二段化し、公式FDSNが空のとき `download_channel_seedlink`（新規）へフォールバック
  - 接続先は `RS_SEEDLINK_HOST` / `RS_SEEDLINK_PORT`（`.env` で上書き可）
  - 本番（発生直後シナリオ）で公式FDSN空→自局SeedLinkから35KB取得成功を確認

### Fixed
- `analyze_rs.py`: 公式FDSN / P2P API への HTTPS を certifi の証明書バンドルで検証するよう修正
  - 原因: python.org 製 Python 3.12.1（本番）で `ssl.get_default_verify_paths()` の cafile/capath が共に None となり、HTTPS が `CERTIFICATE_VERIFY_FAILED` で全滅していた
  - モジュール冒頭で `certifi.where()` から `_SSL_CTX = ssl.create_default_context(cafile=...)` を生成し、全 urlopen（P2P×2・FDSN station・FDSN dataselect）に `context=_SSL_CTX` を渡す
  - certifi 未導入環境では `None` で標準挙動を維持（システム Python 等は無変更）
- `analyze_rs.py`: SeedLink フォールバックで終端が未来の区間を要求するとハングする問題を修正
  - 原因: SeedLink は終端が未来の区間を要求するとデータ到着までブロックし続ける（obspy SeedLink Client の timeout はこの待機に効かない）。WebUI は発生直後の地震を `--duration 420` で解析するため要求区間の終端が未来になりハングしていた
  - `download_channel_seedlink` で `get_waveforms` 前に終端を「現在UTC−5秒」でクランプ。区間全体が未来なら即時 False を返す
  - WebUI 相当（発生直後+duration420）の E2E がローカル12秒・本番14秒で完走することを確認

## リリース手順の実施記録

| 手順 | 実施内容 | 結果 |
|-----|---------|-----|
| 承認確認 | ユーザー「リリースしてください」明示承認 + ローカル/本番E2E立ち会い済 | 確認済 |
| テスト | `pytest src/test_api_events.py src/test_template_parity.py` | 26 passed |
| バージョン番号更新 | バージョン管理ファイルなし（CHANGELOG.md / project_map.md で管理） | スキップ（設計上） |
| CHANGELOG追記 | `## [1.4.0] - 2026-06-16` を docs/CHANGELOG.md 先頭に追記（Added/Fixed） | 完了 |
| project_map.md 更新 | 現バージョン v1.3.0 → v1.4.0、最終更新 2026-06-15 → 2026-06-16 | 完了 |
| gitコミット（docs） | コミットハッシュ: 2bca2cf | 完了 |
| PR作成 | feature/seedlink-fallback → master（#2） | 完了: https://github.com/Masakai/earthQuake/pull/2 |
| master へマージ | `gh pr merge 2 --merge`（マージコミット 0be1304） | 完了 |
| タグ作成 | v1.4.0（注釈付き、master 0be1304 上） | 完了 |
| プッシュ | origin: feature/seedlink-fallback + master + タグ v1.4.0 | 完了 |
| GitHub Release | https://github.com/Masakai/earthQuake/releases/tag/v1.4.0 | 完了 |

## 本番デプロイ状況

- 本番（iMac `/Users/masakai/earthquake/src/analyze_rs.py`）へは scp で転送済み（本番は git 管理外）。本リリース作業の前に完了している。
- バックアップ: `analyze_rs.py.bak.20260616`（SeedLink版前）/ `.bak.20260616-ssl`（SSL修正前）/ `.bak.20260616-hang`（ハング修正前）が本番に存在。
- 本番Webの再起動は `launchctl kickstart -k gui/$UID/com.riverruns.earthquake-web`（KeepAlive 常駐サービスのため kill 不可）。

## 特記事項

- 後方互換の新機能（SeedLinkフォールバック）+ バグ修正のため MINOR バンプ（v1.3.0 → v1.4.0）。
- `requirements.lock.txt`（未追跡）は本リリースのスコープ外。v1.3.0 同様コミットに含めない。
- 教訓（メモリ記録済み）:
  - python.org 製 Python on macOS は証明書バンドル未設定が起きやすい。urllib 系 HTTPS は certifi 経由の SSLContext を明示するのが堅牢。
  - SeedLink は「未来を含む区間」をそのまま渡してはいけない。リアルタイム配信は終端=現在でクランプすること。
- ロールバック手順:
  - 機能取消: master 上で `git revert 24c1f54 9087ea9 ebec4af`（または当該マージコミットを revert）
  - タグ削除: `git push origin :refs/tags/v1.4.0` および `git tag -d v1.4.0`
  - GitHub Release 削除: `gh release delete v1.4.0`
  - 本番ロールバック: `analyze_rs.py.bak.20260616` 系のバックアップから復元 + LaunchAgent 再起動
