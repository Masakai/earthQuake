# リリース記録: v1.5.0

- リリース日: 2026-06-25
- 対象プロジェクト: earthQuake (Masakai/earthQuake)
- バージョン: v1.5.0
- 要件トレーサビリティ: 「ダッシュボード表示が震度1・2を示しても発話されない」という運用上の課題への対応。専用チケットなし（運用者＝ユーザー起点の不具合報告）
- 承認者: ユーザー（「masterへマージして」「リリースしといてね」と明示承認）。発話の動作確認は本番(iMac)でユーザー立ち会いのもと完了済み（テスト発話が実際に音声出力されたことを確認）
- 関連レビュー報告書: なし（reviewer 報告書は未作成。ユーザー立ち会いの実機検証で代替）
- 関連実装仕様書: なし
- 関連設計書: なし（対話の中で方針確定。発話の言い直しは「方式Z＝震度スケールが上がったときだけ言い直す」を採用）
- 技術詳細の出典: `/Users/sakaimasanori/.claude/projects/-Users-sakaimasanori-Dropbox-earthQuake/memory/log_server-changes.md`（2026-06-25 の2エントリ）

## リリース内容

### Changed
- `jma_intensity_tui.py`: 地震警報の発話を STA/LTA トリガから切り離し、ライブの計測震度 `I_final` を監視する方式に変更
  - 背景: 旧実装はトリガ発火の `confirm_window` 秒後（既定10秒）の窓値で発話判定していた。STA/LTA 比のピーク（発火）から 90秒窓の計測震度 I が立ち上がるまで数十秒のラグがあり、発火直後の立ち上がり前の値（震度0）で判定するため、ダッシュボード表示が震度1・2を示しても発話されなかった（トリガ履歴 2226件中 I≥0.5 はわずか3件だった）
  - 遠地地震（S-P 時間が長く発火から震度立ち上がりまで時間がかかる）でも近地地震でも、計測震度が実際に 0.5（震度1相当）を超えた時点で発話する
  - 0.5 超過から `speak_delay` 秒ピークを観測してから初回発話。発話後に震度スケールが1段階以上上がったら、再生中の `say` を `terminate()` して新しい値で言い直す（同一スケール内の数値変動では言い直さない＝途切れ防止）
  - 新引数 `--speak-delay`（既定 2.0秒）を `jma_intensity_tui.py` / `jma_intensity_web.py` に追加
  - `_say_speak` を `_say_popen`（Popen ハンドルを返す非ブロッキング版）に変更し、言い直しのため `AlertSpeaker` にプロセスハンドル（`_cur_proc` / `_proc_lock`）を保持
- `jma_intensity_tui.py`: トリガ履歴に記録する計測震度を、確定時点の窓値から `confirm_window` 期間内のピーク値に変更
  - `pending_queue` の各イベントで `I_final` の最大値を追跡し、確定時にピーク値（とスケール）を `add_event` に渡す

### Added
- `jma_intensity_web.py`: アプリバージョンを定義（`__version__ = "1.5.0"`）し、ダッシュボードのステータスバーに `v1.5.0` を表示。デプロイ反映を画面から確認できるようにした
  - 外部確認用に `GET /api/version`（→ `{"version": "1.5.0"}`）を追加。バージョンはリリース時に git タグと揃えて手動更新する

## リリース手順の実施記録

| 手順 | 実施内容 | 結果 |
|-----|---------|-----|
| 承認確認 | ユーザー「masterへマージして」「リリースしといてね」明示承認 + 本番テスト発話の音声出力をユーザー立ち会いで確認 | 確認済 |
| テスト | `python src/test_template_parity.py` + 発話状態機械・ピーク保持ロジックの単体検証 | 通過（exit 0）/ 全 PASS |
| 実機発話検証 | 本番 iMac で `AlertSpeaker().speak("1", 0.71)` を実行、`say -v Kyoko` が 8.2秒発話し `alert_latency.jsonl` に記録、音声出力をユーザーが確認 | 確認済 |
| バージョン番号更新 | `__version__ = "1.5.0"` を `jma_intensity_web.py` に新設（本リリースから定数化） | 完了 |
| CHANGELOG追記 | `## [1.5.0] - 2026-06-25` を docs/CHANGELOG.md 先頭に追記（Changed/Added） | 完了 |
| project_map.md 更新 | 現バージョン v1.4.0 → v1.5.0 | 完了 |
| master へマージ | ローカルで `git merge --no-ff feature/trigger-peak-intensity`（マージコミット ba22f4c） | 完了 |
| gitコミット（docs） | コミットハッシュ: 1e9568c | 完了 |
| タグ作成 | v1.5.0（注釈付き、master 1e9568c 上） | 完了 |
| プッシュ | origin: master（9a428d1..1e9568c）+ タグ v1.5.0 | 完了 |
| GitHub Release | https://github.com/Masakai/earthQuake/releases/tag/v1.5.0 | 完了 |
| featureブランチ削除 | `git branch -d feature/trigger-peak-intensity`（マージ済み） | 完了 |

## 本番デプロイ状況

- 本番（iMac `/Users/masakai/earthquake/`）へは scp で転送済み（本番は git 管理外）。本リリース作業の前に完了している。
  - `src/jma_intensity_tui.py` + `src/jma_intensity_web.py`（機能本体、コミット bcdbbc8 相当）
  - `src/jma_intensity_web.py` + `src/templates/dashboard.html`（バージョン表示、コミット f9402d6 相当）
- バックアップ（本番）: `jma_intensity_tui.py.bak.20260625` / `jma_intensity_web.py.bak.20260625`（機能本体デプロイ前）、`jma_intensity_web.py.bak.20260625-version` / `dashboard.html.bak.20260625-version`（バージョン表示デプロイ前）
- 本番Webの再起動は `launchctl kickstart -k gui/$UID/com.riverruns.earthquake-web`（KeepAlive 常駐サービスのため kill 不可）。再起動後 `GET /` 200・`GET /api/version` で `{"version":"1.5.0"}`・HTML に `v1.5.0` 出力を確認。

## 特記事項

- 後方互換の動作改善（発話ロジック分離）+ 新機能（バージョン表示）のため MINOR バンプ（v1.4.0 → v1.5.0）。
- `requirements.lock.txt`（未追跡）は本リリースのスコープ外。コミットに含めない。
- 残作業（設定・運用、コード変更なし）:
  - トリガ閾値（`trig`）を下げて遠地地震の見逃しを減らす（ダッシュボード ⚙ 設定で実施）
  - `speak_delay`（既定2秒）を運用で調整
- 未検証事項: 実トリガ（次に I_final が 0.5 を超える揺れ）での発話は未発生。発話エンジン単体・ロジック・常駐経路（6/16 に実トリガ発話の記録あり）は検証済みだが、新ロジックでの実トリガ発話は次の揺れ待ち。
- ロールバック手順:
  - 機能取消: master 上で `git revert ba22f4c`（マージコミットを revert）
  - タグ削除: `git push origin :refs/tags/v1.5.0` および `git tag -d v1.5.0`
  - GitHub Release 削除: `gh release delete v1.5.0`
  - 本番ロールバック: `*.bak.20260625` 系のバックアップから復元 + LaunchAgent 再起動
